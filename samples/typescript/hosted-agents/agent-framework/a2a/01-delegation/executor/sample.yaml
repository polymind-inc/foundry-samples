name: A2A Executor Hosted Agent
description: Hosted Agent Framework sample (Responses protocol) that acts as a math-expert specialist exposed as an Agent-to-Agent (A2A) endpoint via the companion setup-a2a script.

build: "npm install && npm run build"
validate: "npm run typecheck"
